# 页面加载与音乐播放修复

## 2026-09-08：首屏与站内导航优化

- 字体文件统一独立输出，浏览器根据页面实际字符按需请求。原先较小的字库分片会被内嵌进全站 CSS，未使用的字符也占用首屏带宽；现在仍使用原有完整字体及 Unicode 分片，无需裁字或更换字体。
- 用单组委托事件处理悬停、键盘聚焦和按下链接：提前缓存目标 HTML 及最多四份尚未加载的页面样式。只解析目标页面的 head，不加载正文图片、执行目标脚本或启动音乐；首页不批量预取栏目。省流量、2G、离线或隐藏页面不做预取，预取失败仍可正常点击导航。
- 搜索资源加载的全局监听只注册一次，避免站内切页后重复累积。

以下为 Chrome 本地正式构建的三次测试中位数。测试服务启用 gzip 与 HTTP 缓存，每轮使用新浏览器上下文，模拟 100 ms 延迟、1.6 Mbps 下载、4 倍 CPU 降速；切页测试为悬停项目链接 1.5 秒后点击，记录到 Astro 页面就绪。

| 指标 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 首页外部 CSS（解压后） | 319,047 B | 242,119 B（−24.1%） |
| 首次内容绘制 FCP | 1,820 ms | 1,532 ms（−15.8%） |
| 最大内容绘制 LCP | 2,100 ms | 1,804 ms（−14.1%） |
| 悬停后进入项目页 | 408 ms | 293 ms（−28.3%） |
| 布局偏移 CLS | 0.00201 | 0.00201 |

独立字体请求增加约 6.9 KB，是原内嵌且当前页面实际使用的分片移出 CSS 的结果；其余未使用分片不再随 CSS 下载。以上是模拟网络下的样本，并非公网或所有页面的速度承诺；立即点击、触屏、首次访问文章等场景的收益不同。

验证通过：正式构建、类型检查、预加载与省流量模式、连续音乐播放/重试/历史导航、触屏操作、四季动画跨页面连续性、浅深色及减少动态效果模式。预览使用最新 dist，未发布线上。

复测：构建后运行 `pnpm benchmark`，结果写入 `node_modules/.cache/performance/latest.json`。可用 `node script/benchmark-browser.mjs before` / `after` 保存两份对照。功能回归使用 `pnpm test:browser` 与 `pnpm test:seasonal`。

## 修改内容

- 将整页所有链接的加载后预取改为悬停／聚焦时预取，减少首屏和音乐播放时的带宽竞争。继续使用 Astro 客户端导航和共享资源缓存。
- 拆分样式，首页不再下载文章使用的 KaTeX、代码块及图片查看器样式；减少非必要字体预加载，英文页面不再强制预加载中文字库分片。
- 首屏文字直接显示，滚动动画仅应用于屏幕外内容；站内导航跳过入场动画及不必要的观察器创建。
- 音频实例脱离会被替换的页面 DOM，随持久化播放器保留；点击前不下载音频。
- 避免因 `currentSrc` 暂时为空而重复加载；用请求序号隔离旧播放请求，避免快速切歌产生的拒绝覆盖新曲目状态。
- 修复媒体报错但 `paused` 仍为 `false` 时，重试点击被误判为暂停的问题。暂停、切歌、加载失败与播放进度恢复分别处理。
- 完整刷新恢复曲目和位置，等待点击继续；站内切换保持播放。
- 构建时将普通 M4A 的索引前移，保持源文件与音频数据不变；不支持的封装保留原样并提示。

## 验证

首页外部 CSS 未压缩体积由 **391,665 字节降至 314,961 字节**，减少 **19.6%**。这是资源体积对比，不是线上加载耗时承诺；实际响应时间仍取决于网络与 GitHub Pages 服务。

- 正式构建生成 13 个页面，Pagefind 索引覆盖中英文。
- 类型检查及三首音频的索引位置、数据一致性、块指针与重复运行校验。
- Chrome 实际音频播放：首屏无音频请求、无全站链接预取、连续切页、前进后退、快速切歌、刷新后续播、过期请求拒绝、请求失败后重试。
- Chrome 触屏模拟：播放、切歌、暂停；关闭 JavaScript 后正文仍可见。

复现命令（Node 22+，浏览器测试默认使用已安装的 Chrome）：

```sh
pnpm install --frozen-lockfile
pnpm build
pnpm typecheck
pnpm test:audio
pnpm test:browser
```

可用 `BROWSER_CHANNEL=msedge` 改用本机 Edge。触屏模拟不替代真机 Safari/iOS 验证；本次未发布到线上，也未测量公网访问耗时。

实现依据：[Astro 预取策略](https://docs.astro.build/en/guides/prefetch/)、[MDN 音频 load() 对待处理 play() 请求的影响](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/load)。
