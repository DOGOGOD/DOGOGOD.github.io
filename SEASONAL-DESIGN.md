# 四季随机动画

沿用原有的纯色暖纸与深色背景、衬线字体和陶土色。已移除窗影、柔光及相关渐变与监听器。文章正文中间保持干净，偶发动画偏向两侧留白。全部图形由 CSS 绘制，不增加图片请求或动画库。

## 四季与主题

按访问者设备的当前月份采用北半球四季：3–5 月春、6–8 月夏、9–11 月秋、12–2 月冬。首页中英文问候语同时更新，不依赖网站重新构建。

| 季节 | 偶发动画 |
| --- | --- |
| 春 | 带瓣尖缺口与花心的灰粉五瓣樱花 |
| 夏 | 淡金色短流星 |
| 秋 | 赭色叶片 |
| 冬 | 灰白六角雪花 |

四季图形均有浅色和深色配色，切换主题时跟随更新。

## 节奏与控制

- 进入页面后约 5–12 秒出现首组动画，此后每隔 24–52 秒随机触发。每片花瓣／叶片／雪花有独立的起点、摆幅、风向、速度、旋转和延迟；流星有独立的起点、角度、长度与速度。
- 桌面每组 4–7 个，手机 3–4 个；流星每次 2–3 颗，在半秒内错落出现。同时存在的数量上限分别为 9 和 5。
- 页脚叶子按钮可暂停／恢复动态效果，选择保存在本地。系统减少动态效果优先，不可由按钮覆盖。
- 站内切页保留同一动画层、粒子、播放进度与随机计时，只更新新页面的语言和按钮。进入文章时仅调整后续粒子的生成范围，已有粒子不突然消失或跳位。后台标签暂停，回来重新安排时间，避免补发或叠加。装饰层不接收鼠标、触摸、焦点，不进入辅助技术阅读顺序。

## 调整与检查

- `src/styles/seasonal-atmosphere.css`：四季图形与深浅配色、正文避让、手机与减少动态效果。
- `src/scripts/seasonal-atmosphere.ts`：随机节奏、轨迹、数量上限、生命周期与暂停开关。
- `src/utils/season.ts`：月份与四季映射、中英文问候语。
- 构建后运行 `npm run test:seasonal`。使用 Playwright 模拟四季日期，检查深浅主题、随机路径、切页及前进后退时元素和播放进度连续、后台暂停、手机上限、正文避让及减少动态效果。截图保存在忽略的 `node_modules/.cache/seasonal/` 中。
